name: itibb_scanner
description: Aplicación de adquisición de datos industrial para el ITI-BB.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  mobile_scanner: ^3.5.0
  http: ^1.1.0
  cupertino_icons: ^1.0.2
  # Nuevas dependencias para Sincro v2.2.3
  cached_network_image: ^3.3.1
  google_fonts: ^6.2.1
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true